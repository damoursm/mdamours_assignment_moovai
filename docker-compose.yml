version: '3.8'

services:
  market-agent:
    build: .
    container_name: market-analysis-agent
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
    restart: unless-stopped